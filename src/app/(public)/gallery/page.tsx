import { GalleryScreen } from "@/frontend/screens/public/gallery-screen";

export const metadata = { title: "Gallery — Parallax" };

export default function GalleryPage() {
  return <GalleryScreen />;
}
