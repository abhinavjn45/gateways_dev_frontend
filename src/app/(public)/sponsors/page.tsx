import { SponsorsScreen } from "@/frontend/screens/public/sponsors-screen";

export const metadata = { title: "Sponsors — Parallax" };

export default function SponsorsPage() {
  return <SponsorsScreen />;
}
